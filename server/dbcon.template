var mysql = require('promise-mysql');
var pool = mysql.createPool({
    connectionLimit: 10,
    host: 'localhost',
    user: 'node',
    password: 'cs340',
    database: 'neighbor'
});

module.exports.pool = pool;